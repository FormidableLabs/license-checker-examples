/**
 * URQL-based app.
 */

import { createClient, Provider } from "urql";
import React from "react";
import ReactDOM from "react-dom";

console.log({
  React,
  ReactDOM,
  createClient,
  Provider
});
